# aoc2025

To install dependencies, run `bun install`.

To run all days with test data, run `bun test`.

To run a particular day with real input, e.g. day 1, first download `input/d01` and then run `bun d01`.
